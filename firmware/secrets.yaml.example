# Kopieren nach secrets.yaml und ausfuellen. secrets.yaml ist gitignored.
#
#   ota_password:  frei waehlbar, einmal setzen und merken
#   api_key:       ESPHome-Verschluesselungskey, 32 Byte base64. Erzeugen mit
#                  python3 -c "import base64,os;print(base64.b64encode(os.urandom(32)).decode())"
wifi_ssid: "DeinWLAN"
wifi_password: "..."

ota_password: "..."
api_key: "..."

# WebSocket-Adresse der Bridge. Der abschliessende Slash gehoert dazu.
va_url: "ws://192.168.1.50:8765/"
